apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: caddy-tracker-data
  name: caddy-tracker-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
