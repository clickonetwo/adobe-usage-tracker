apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.clickonetwo.service: tracker
  name: tracker-data-volume
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
